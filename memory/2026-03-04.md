# 2026-03-04

## DSE trading automation updates
- Reviewed dse-trading pre-market deterministic pipeline; fixed **circuit semantics**:
  - bundler now outputs `circuit_limits_codes` (universe) vs `circuit_hit_codes` (upper/lower hits) + detailed `circuit_hits`.
  - planner/analyst prompts updated to hard-exclude only `circuit_hit_codes` (not the whole limits universe).
- Added/confirmed ripgrep (`rg`) installed and usable (path `/opt/homebrew/bin/rg`, v15.1.0).

## Cron jobs
- Added cron: **DSE pre-market** (Sun–Thu 09:00 Asia/Dhaka) → Telegram chatId `1543420530`.
  - Job ID: `9c0b2ba0-4199-4bf5-a845-ae1bc18d42e6`.
  - Ran test successfully; holdings were initially empty due to broker call timeout.
- Fixed pre-market holdings emptiness cause:
  - `dse-ai broker shanta portfolio --json` sometimes exceeded per-command timeout.
  - Updated `scripts/pre-market/bundler.py`: broker portfolio call uses longer timeout (`timeout_s=120`).
  - Re-run showed holdings populated.

## Market-open pipeline + newsletter integration
- Created deterministic `market-open/` pipeline (runner/bundler/enrich + planner/analyst prompts).
- Added newsletter step for market-open:
  - Uses `skills/dse-trading/scripts/newsletter.py` (Gmail via `gog`) to fetch yesterday’s newsletter.
  - Added `prompts/market-open/newsletter-brief.md` to compress raw newsletter to ≤12 lines for planner.
- Added cron: **DSE market-open** (Sun–Thu 10:40 Asia/Dhaka) → Telegram `1543420530`.
  - Job ID: `086beb28-5a34-4c45-acac-bc068045271b`.
  - Ran test successfully; newsletter brief + strict planner JSON worked end-to-end.

## Mid-market
- Created deterministic `mid-market/` pipeline (copied from market-open; phase renamed) + prompts.
- Added cron: **DSE mid-market** (Sun–Thu 12:30 Asia/Dhaka) → Telegram `1543420530`.
  - Job ID: `a51e0c88-4c63-4b3d-8396-19b94aa07741`.
  - Test run succeeded but holdings_table was empty because `dse-ai broker shanta portfolio --json` prompted interactively: `Enter password for D00211:` (cron can’t answer). Needs non-interactive auth/session or cached snapshot approach.
